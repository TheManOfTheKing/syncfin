# Configuração Nixpacks para Railway
# Railway usa Nixpacks para detectar e buildar o projeto

[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build:server"]

[start]
cmd = "npm start"

